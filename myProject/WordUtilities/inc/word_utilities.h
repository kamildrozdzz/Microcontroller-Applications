#ifndef WORD_UTILITIES_H_
#define WORD_UTILITIES_H_

/**
 * Searches for the longest word within the str string argument and returns its length.
 * @param[in] str text string to search
 * @return number of characters in the longest word
 */
int GetLongestWordLen(const void* str);

#endif /* WORD_UTILITIES_H_ */
